apiVersion: v1
kind: Service
metadata:
  name: app-service
  labels:
    app: app-dep
    job: node-api
spec:
  selector:
    app: app-dep
  ports:
    - port: 8080
      targetPort: 8080
      name: web
      protocol: TCP
  type: ClusterIP
